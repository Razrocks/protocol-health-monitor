# Database Configuration
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/protocol_health
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=protocol_health

# API Configuration
FLASK_ENV=development
FLASK_DEBUG=True

# Optional: GitHub SHA for run tracking (auto-set in CI)
GITHUB_SHA=
